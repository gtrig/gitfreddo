# Optional overrides (also configurable in ~/.config/gitfredo/settings.json)
# GIT_BINARY=/usr/bin/git

# GitHub OAuth App client ID (Device Flow enabled, repo scope)
# GITHUB_CLIENT_ID=your_oauth_app_client_id
