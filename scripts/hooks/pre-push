#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "$0")/../.." && pwd)"

if ! git -C "$ROOT" rev-parse --git-dir >/dev/null 2>&1; then
  exit 0
fi

exec bash "$ROOT/scripts/run-ts.sh" "$ROOT/scripts/check-release-pre-push.ts" < /dev/stdin
